#include<stdio.h>
int main()
{
    int i,j=0,n,m,rem,rem1;
    scanf("%d",&n);
    m=n;
    for(i=0;n>0;i++)
    {
        {
        if rem==0)
            i--;
        }
    }
    while(m>0)
    {
        j++;
        rem1=m%10;
        m=m/10;
        if(rem1==0)
        {
            j--;
        }
    }
    printf("using for loop value is %d \nat the same time using while loop %d\n",i,j);
    return 0;
}
